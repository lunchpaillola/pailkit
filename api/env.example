# PailKit API Environment Variables
# Copy this file to .env and fill in your actual API keys

# Daily.co API Key
DAILY_API_KEY=your-daily-api-key-here

# Unkey (API access + verification)
# Root key used by the server to verify incoming PailKit keys
UNKEY_ROOT_KEY=your-unkey-root-key-here
# API identifier from Unkey (optional for now, reserved for future use)
UNKEY_API_ID=your-unkey-api-id-here

UNKEY_PAILKIT_SECRET=

# Sentry Error Tracking
# DSN (Data Source Name) from your Sentry project
# Get this from: https://sentry.io/settings/[your-org]/projects/[your-project]/keys/
# Format: https://[key]@[org].ingest.sentry.io/[project-id]
# Leave empty to disable Sentry (useful for local development)
SENTRY_DSN=your-sentry-dsn-here
# Optional: Set the environment name (e.g., "production", "staging", "development")
SENTRY_ENVIRONMENT=production
# Optional: Set the release version (e.g., git commit hash, version number)
# SENTRY_RELEASE=
