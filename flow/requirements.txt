# Copyright 2025 Lunch Pail Labs, LLC
# Licensed under the Apache License, Version 2.0

# FastMCP and core dependencies
mcp>=0.1.0
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.8.0
python-dotenv>=1.0.0
unkey-py>=0.3.0
langgraph==0.2.40

# Pipecat for AI bot functionality
# Includes: daily (Daily.co transport), openai (LLM/TTS), local-smart-turn-v3 (turn detection), deepgram (STT)
pipecat-ai[daily,openai,local-smart-turn-v3,deepgram]>=0.0.94

# Modal client (required when USE_MODAL_BOTS=true)
modal>=0.60

# OpenAI SDK for direct API access (used in ExtractInsightsStep)
openai>=1.0.0

# PostHog LLM Analytics SDK for automatic LLM usage tracking
posthog[ai]>=3.0.0

# Image processing for sprite animation
Pillow>=10.0.0

# Email service
resend>=2.0.0

# Field-level encryption
cryptography>=42.0.0

# Supabase client for PostgreSQL database
supabase>=2.0.0
# PostgreSQL driver for LangGraph checkpointer (pool extra needed for async)
psycopg[binary,pool]>=3.1.0
# LangGraph PostgreSQL checkpointer (async support)
langgraph-checkpoint-postgres>=0.1.0

# HTTP client for Fly.io API calls (async httpx is used, requests kept for compatibility)
requests>=2.31.0
httpx>=0.25.0
