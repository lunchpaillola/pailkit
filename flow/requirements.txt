# Copyright 2025 Lunch Pail Labs, LLC
# Licensed under the Apache License, Version 2.0

# FastMCP and core dependencies
mcp>=0.1.0
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.8.0
python-dotenv>=1.0.0
unkey-py>=0.3.0
langgraph==0.2.40

# Pipecat for AI bot functionality
# Includes: daily (Daily.co transport), openai (LLM/TTS), local-smart-turn-v3 (turn detection), deepgram (STT)
pipecat-ai[daily,openai,local-smart-turn-v3,deepgram]>=0.0.94

# OpenAI SDK for direct API access (used in ExtractInsightsStep)
openai>=1.0.0

# Image processing for sprite animation
Pillow>=10.0.0

# Email service
resend>=2.0.0

# Field-level encryption
cryptography>=42.0.0

# Supabase client for PostgreSQL database
supabase>=2.0.0
# PostgreSQL driver for LangGraph checkpointer
psycopg[binary]>=3.1.0

# HTTP client for Fly.io API calls (synchronous requests)
requests>=2.31.0

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
mypy>=1.5.0
ruff>=0.3.0
pre-commit>=3.5.0
